# AI 审计管理办法

## 1. 总则

### 1.1 目的

为规范使用 AI 对软件项目开展代码审计、质量审计、安全审计和交付审计的工作过程，保证审计结论基于证据、规则一致、可复核、可追踪、可整改，制定本办法。

本办法依据 `code-audit-report` skill 的审计原则、审计流程、评分体系、报告结构、误报与基线管理规则整理，适用于以 AI 辅助人工审计的代码仓库、交付版本、增量变更、CI 门禁和外部交付前质量评估。

### 1.2 适用范围

本办法适用于以下审计场景：

1. 全量代码仓库审计。
2. 指定分支、提交范围或 Pull Request 的增量审计。
3. 发布前质量、安全和交付准备度审计。
4. 研发过程中的专项审计，例如安全专项、架构专项、可靠性专项、供应链专项。
5. 已有工具结果的复核审计，例如 SARIF、Semgrep、Gitleaks、依赖漏洞扫描、SBOM 结果导入后的人工确认。

本办法覆盖 Java、C/C++、C#、Shell、BAT/CMD、Kotlin、Blazor、XAML/WPF、CSS、Lua、PHP、Python、Go、JavaScript/TypeScript、SQL、配置文件、CI 文件、容器文件及混合语言项目。

### 1.3 审计原则

1. 证据优先。每一项问题必须基于源代码、配置、依赖清单、工具输出、提交记录、设计资料或可复现行为。
2. 禁止编造。AI 不得在缺少证据的情况下推断为确定问题；证据不足但存在合理怀疑的，应标记为“待复核”。
3. 定位明确。优先提供文件路径和行号；无法提供行号时，应提供类、方法、函数、配置键、命令或依赖坐标。
4. 风险优先。安全、功能正确性、可靠性和架构风险优先于纯风格问题。
5. 根因归并。重复问题应按根因合并，不按每个重复位置机械堆叠。
6. 可追踪。问题应具有稳定的规则 ID、指纹、状态、责任人、整改建议和复核记录。
7. 不静默修改。审计过程中不得擅自修改被审计项目源码、生产配置、依赖版本或 CI 文件，除非用户明确要求修复。
8. 工具输出需复核。静态扫描、漏洞扫描和 AI 判断均不得直接等同于最终结论，必须结合上下文复核。

## 2. 角色职责

### 2.1 审计发起人

审计发起人负责明确审计目标、范围、版本、交付时间和验收标准，提供必要的业务背景、权限模型、运行环境、构建方式和已知风险。

### 2.2 AI 审计执行者

AI 审计执行者负责执行项目盘点、规则匹配、工具调用、结果分析、人工复核辅助、报告生成和整改建议整理。AI 审计执行者必须标明证据来源、置信度、限制项和待复核项。

### 2.3 技术负责人

技术负责人负责确认架构、核心业务逻辑、数据一致性、运行环境约束和整改可行性，对高风险和中风险问题给出处理意见。

### 2.4 安全负责人

安全负责人负责确认漏洞等级、可利用性、CWE/OWASP 映射、补偿控制、风险接受期限和发布门禁策略。

### 2.5 整改责任人

整改责任人负责按优先级修复问题、补充测试、提交复测材料，并在风险接受时说明业务理由、缓解措施和有效期限。

## 3. 审计流程

### 3.1 范围确认

审计开始前应确认以下信息：

1. 仓库路径、分支、提交范围或变更基线。
2. 主要语言、框架、构建系统和运行平台。
3. 审计范围和排除范围，例如生成代码、第三方 vendored 代码、测试夹具、历史归档目录。
4. 审计重点，例如安全、架构、可靠性、发布准备度、供应链。
5. 交付形式，例如结构化 JSON、离线 HTML 报告、整改清单、CI 门禁结果。
6. 是否启用增量审计、基线管理、SBOM、脱敏报告和 CI 门禁。

### 3.2 项目盘点

项目盘点应至少记录：

1. 文件总数、源码文件数、被排除文件数、源码行数。
2. 语言分布、扩展名分布、核心目录和入口点。
3. 构建系统、依赖清单、锁文件、容器文件、CI 文件。
4. 测试目录、测试文件数量、测试框架。
5. 配置文件、密钥风险提示、外部接口、数据访问点、命令执行点。
6. 已有工具配置，例如 `.editorconfig`、静态分析配置、测试配置、安全扫描配置。

### 3.3 工具审计

工具审计按以下顺序优先执行：

1. 构建或编译检查。
2. 单元测试和集成测试。
3. 语言静态分析、代码规范检查、类型检查。
4. 依赖漏洞、许可证、SBOM 和容器镜像扫描。
5. 密钥扫描。
6. 安全规则和高风险模式扫描。

工具记录应包含工具名称、版本、命令、退出码、耗时、输出文件、成功或失败状态、失败原因和跳过原因。

### 3.4 AI 复核与人工确认

AI 应重点复核以下区域：

1. 控制器、处理器、命令行入口、定时任务、消息消费者等公开入口。
2. 认证、授权、权限边界、租户隔离和越权风险。
3. 输入校验、输出编码、序列化、反序列化、SQL、ORM、文件、进程、网络和 Shell 操作。
4. 事务边界、重试、降级、回滚、幂等和数据一致性。
5. 业务状态转换、默认值、fallback、异常分支和边界条件。
6. 日志、指标、错误码、审计日志、健康检查和告警。
7. 依赖清单、锁文件、构建插件、容器镜像、下载脚本和工具链信任。

### 3.5 问题分类

每个问题应包含：

1. 标题。
2. 严重等级。
3. 审计维度。
4. 优先级。
5. 置信度及置信度理由。
6. 位置和证据。
7. 影响说明。
8. 整改建议。
9. 规则 ID、来源、指纹、状态。
10. 影响组件、修复工作量、可选 CWE/OWASP 映射和修复示例。

### 3.6 报告交付

审计报告应包含：

1. 审计概况。
2. 总体结论。
3. 审计配置与交付选项。
4. 项目盘点。
5. 增量审计信息。
6. 误报与基线管理。
7. 供应链与 SBOM。
8. 风险与问题统计。
9. 维度评分明细。
10. 高风险问题、中风险问题、低风险问题与优化建议。
11. 待复核项。
12. 整改优先级与路线图。
13. 工具与命令记录。
14. 未覆盖范围与限制项。

## 4. 评分与门禁管理

### 4.1 默认权重

审计采用 100 分制，默认权重如下：

| 维度 | 权重 |
|---|---:|
| 安全性 | 20 |
| 功能正确性 | 15 |
| 可靠性与健壮性 | 15 |
| 架构合理性 | 15 |
| 编码规范性 | 10 |
| 可维护性与可演进性 | 10 |
| 测试质量 | 5 |
| 性能与资源 | 5 |
| 依赖与供应链风险 | 3 |
| 可观测性与运维性 | 2 |

如项目领域特殊，可调整权重，但必须在报告中记录调整原因。例如，嵌入式、网关、守护进程、设备控制系统可提高可靠性、可观测性和性能权重；互联网外部暴露系统可提高安全性、供应链和可观测性权重。

### 4.2 严重等级

| 等级 | 判定标准 |
|---|---|
| 高 | 可被利用的安全漏洞、数据丢失或损坏、鉴权绕过、重大可用性风险、严重业务结果错误、生产事故高概率触发点。 |
| 中 | 有明确用户影响的正确性或可靠性问题，阻碍安全演进的维护性问题，中等安全加固缺口，关键运维信号缺失。 |
| 低 | 局部可维护性、风格、文档、测试补强、可观测性或小型性能问题，影响范围有限。 |
| 待复核 | 存在合理怀疑但证据不足，需要运行环境、业务约束、权限模型或数据样本进一步确认。 |

### 4.3 优先级

| 优先级 | 处理要求 |
|---|---|
| P0 | 必须在发布或上线前修复。 |
| P1 | 应在最近一个修复窗口内处理。 |
| P2 | 应进入迭代计划并指定责任人。 |
| P3 | 可作为持续改进项处理。 |

### 4.4 置信度

| 置信度 | 判定标准 |
|---|---|
| 高 | 证据直接来自源代码、配置、依赖清单或可复现工具输出。 |
| 中 | 证据明确，但最终影响依赖运行配置、输入来源、数据样本或部署拓扑。 |
| 低 | 属于风险提示，需要进一步复核。 |

### 4.5 扣分规则

1. 从各维度满分开始扣分。
2. 按根因扣分，不按重复出现次数机械扣分。
3. 高风险问题通常在对应维度扣 5 至 8 分。
4. 中风险问题通常在对应维度扣 2 至 4 分。
5. 低风险问题通常在对应维度扣 0.5 至 1.5 分。
6. 待复核项通常扣 0 至 1 分，视证据强度决定。
7. 单一维度扣分不得超过该维度权重。
8. 因环境缺失导致工具无法运行时，应记录为限制项，不得编造工具发现。

### 4.6 总体评级

| 分数 | 评级 | 管理含义 |
|---:|---|---|
| 90-100 | 优秀 | 仅存在少量低风险优化项。 |
| 80-89 | 良好 | 存在明确改进项，但整体风险可控。 |
| 70-79 | 一般 | 存在中风险问题或工程质量短板。 |
| 60-69 | 较差 | 存在多项中高风险问题，需要集中整改。 |
| 60 以下 | 高风险 | 不建议在未整改前发布或对外交付。 |

## 5. 10 个维度审计规则

### 5.1 架构合理性

#### 管理目标

确保系统分层清晰、模块边界稳定、依赖方向合理、事务和状态边界明确，能够支撑当前业务和后续演进，避免架构问题引发连锁质量风险。

#### 审计规则

1. 分层规则。表现层、应用层、领域层、基础设施层、数据访问层等边界应清晰，不应出现跨层直接调用、底层依赖上层或业务逻辑散落在基础设施代码中的情况。
2. 依赖方向规则。依赖应从高层策略流向低层实现或通过接口反转，禁止形成不必要的循环依赖。
3. 模块边界规则。模块职责应单一，公开接口应稳定，内部实现不得被外部模块直接依赖。
4. 事务边界规则。涉及数据库、消息、缓存、外部调用的业务流程，应明确事务范围、提交时机、回滚策略和失败补偿。
5. 状态边界规则。跨模块共享状态应有明确所有者和同步策略，禁止隐式全局状态驱动核心流程。
6. 缓存设计规则。缓存键、失效策略、一致性策略、穿透保护、雪崩保护和降级行为应有明确设计。
7. 分布式失败规则。跨服务调用应考虑超时、重试、限流、熔断、幂等、补偿和链路追踪。
8. 配置设计规则。配置应集中管理、类型明确、默认值安全，生产敏感配置不得硬编码。
9. 扩展点规则。插件、策略、回调和接口扩展应有清晰契约，避免通过反射、字符串或隐式约定造成不可控耦合。
10. 演进兼容规则。公共 API、数据库结构、消息格式、配置格式变更应考虑兼容性和迁移路径。

#### 证据要求

应收集架构图、目录结构、依赖关系、核心模块接口、配置文件、事务代码、缓存代码、服务调用代码、数据库迁移脚本、提交记录和相关设计说明。

#### AI 判定口径

AI 应优先识别循环依赖、跨层调用、业务逻辑错位、全局状态、事务范围过大或过小、缓存与数据源不一致、配置分散和公共接口不稳定等问题。缺少业务设计材料时，应标记为“待复核”，不得仅凭命名风格判定架构不合理。

#### 整改要求

高风险架构问题应在发布前形成整改方案或风险接受记录；中风险问题应纳入迭代计划；低风险问题可作为架构治理项持续改进。涉及模块拆分、事务重构和接口调整的，应补充回归测试和迁移方案。

### 5.2 功能正确性

#### 管理目标

确保代码实现符合业务规则，状态转换、边界条件、异常分支、默认值和 fallback 行为不会导致错误业务结果、数据不一致或用户权益受损。

#### 审计规则

1. 业务规则规则。核心业务规则应在代码中明确表达，并有对应测试或业务文档支撑。
2. 状态转换规则。订单、任务、审批、支付、设备状态等核心状态应有合法迁移约束，禁止任意跳转或遗漏终态。
3. 边界条件规则。空值、零值、最大值、最小值、重复输入、过期数据、分页边界和时间边界应被覆盖。
4. 默认值规则。默认值必须业务安全，未知输入优先进入 unknown、error 或 reject 状态，不应默认为正常业务值。
5. fallback 规则。降级和 fallback 应明确触发条件、影响范围、日志和告警，禁止静默返回误导性成功结果。
6. 数据一致性规则。跨表、跨服务、跨缓存、跨消息的数据变更应保证一致性或具备补偿机制。
7. 异常分支规则。异常处理不得吞掉错误，不得在失败时继续执行后续关键业务步骤。
8. 时间规则。时区、夏令时、日期截断、超时、定时任务和有效期计算应明确。
9. 并发业务规则。重复提交、并发修改、幂等请求和竞态条件应有明确处理。
10. 回归规则。历史缺陷对应路径应有回归测试或明确复测记录。

#### 证据要求

应收集业务规则文档、状态定义、状态迁移代码、核心算法、边界判断、异常分支、数据库约束、测试用例、缺陷记录和生产问题复盘。

#### AI 判定口径

AI 应重点检查状态机缺失、条件判断遗漏、默认值不安全、错误分支返回成功、异常被吞掉、数据写入顺序不合理、重复请求导致重复扣款或重复执行等问题。业务规则不明确时，应列为“待复核”并说明需要补充的业务证据。

#### 整改要求

涉及资金、权限、库存、审批、设备控制、数据删除和用户权益的正确性问题应优先处理。整改应补充边界测试、异常测试、并发测试和回归测试。

### 5.3 编码规范性

#### 管理目标

确保代码符合语言规范、团队规范和通用工程实践，降低阅读成本、误用风险和长期维护成本。

#### 审计规则

1. 命名规则。类、方法、变量、常量、配置键和文件命名应清晰表达含义并符合语言习惯。
2. 注释规则。注释应解释业务意图、约束和复杂逻辑，不应重复代码表面含义。
3. 复杂度规则。函数、类和条件分支复杂度应受控，复杂逻辑应拆分为可测试单元。
4. 异常处理规则。异常类型、捕获范围、错误传播和恢复动作应明确，禁止泛化捕获后静默忽略。
5. 资源释放规则。文件、网络连接、数据库连接、锁、线程、句柄和临时资源应正确释放。
6. 魔法值规则。业务常量、状态码、阈值、路径和时间单位应命名化、配置化或集中定义。
7. 重复代码规则。重复逻辑应抽取为共享函数、策略或组件，避免多处修改不一致。
8. 语言惯用规则。应遵守对应语言的类型、安全、并发、内存、格式化和包管理惯例。
9. 可读性规则。代码布局、缩进、格式、导入顺序和表达式结构应便于审阅。
10. 死代码规则。无用分支、未使用变量、废弃接口和不可达代码应清理或标注迁移计划。

#### 证据要求

应收集代码片段、静态分析结果、格式化配置、语言规范参考、重复代码位置、复杂度指标、资源使用位置和异常处理路径。

#### AI 判定口径

AI 不应把个人偏好当作规范问题。只有当编码风格违反项目既有规范、语言通用规范，或明显影响可维护性、正确性、安全性时，才应进入正式问题清单。纯风格问题一般归为低风险。

#### 整改要求

整改应优先处理会引发资源泄露、异常丢失、误用接口、复杂度过高和重复逻辑不一致的问题。格式化类问题应尽量通过统一工具批量处理。

### 5.4 安全性

#### 管理目标

识别和控制代码、配置、依赖和运行流程中的安全风险，重点覆盖 OWASP Top 10、CWE 常见漏洞、认证授权、注入、敏感信息、路径访问、反序列化和加密错误。

#### 审计规则

1. 认证规则。登录、令牌、会话、签名、回调和服务间认证应有明确校验，不得信任未验证来源。
2. 授权规则。每个敏感操作应校验用户、角色、租户、资源归属和操作权限，禁止只在前端或入口层做权限判断。
3. 注入规则。SQL、命令、模板、表达式、LDAP、NoSQL、ORM 动态查询等不得通过不可信输入拼接执行。
4. 命令执行规则。禁止 shell 字符串拼接执行外部命令；确需执行时应使用参数数组接口，校验可执行文件和参数白名单，并记录退出码和错误输出。
5. XSS 与输出编码规则。用户可控内容进入 HTML、JS、CSS、URL、Markdown 或富文本时，应按上下文编码或净化。
6. SSRF 规则。服务端发起 URL 请求时，应限制协议、域名、IP 段、跳转和 DNS 解析结果。
7. 路径穿越规则。文件路径应规范化后校验仍位于允许根目录内，拒绝绝对路径、上级目录跳转和危险符号。
8. 反序列化规则。不得对不可信输入执行原生对象反序列化；确需反序列化时应使用安全格式、schema 和类型白名单。
9. 敏感信息规则。密钥、令牌、密码、证书、连接串和个人敏感信息不得硬编码、写入日志或提交到仓库。
10. 加密规则。禁止使用弱算法、固定 IV、硬编码密钥、不安全随机数和明文传输。
11. 文件上传规则。上传文件应限制类型、大小、扩展名、内容、存储路径和访问权限。
12. 审计日志规则。关键安全事件应记录审计日志，并避免泄露敏感字段。

#### 证据要求

应收集入口代码、权限校验点、数据库查询、命令执行点、文件操作点、网络请求点、序列化 API、配置文件、日志代码、依赖扫描结果、密钥扫描结果和安全工具输出。

#### AI 判定口径

AI 应把可利用路径、输入来源、权限上下文和防护措施作为判定核心。只有发现调用点、输入来源和缺少有效校验时，才应判定为确定安全问题；缺少运行环境或权限模型证据时，应标记为“待复核”。

#### 整改要求

高风险安全问题原则上不得带病发布。确需风险接受时，必须有安全负责人批准、补偿控制、责任人、有效期限和复审日期。涉及密钥泄露的，应立即轮换凭据并清理历史暴露风险。

### 5.5 可靠性与健壮性

#### 管理目标

确保系统在异常输入、依赖失败、并发冲突、资源耗尽、网络抖动和关闭重启场景下能够稳定运行、可恢复、可定位。

#### 审计规则

1. 空值与边界规则。所有外部输入、查询结果、配置值和可选字段应进行空值和边界处理。
2. 超时规则。外部 I/O、远程调用、队列等待、锁等待和长耗时任务必须有超时、取消和恢复路径。
3. 重试规则。重试应有次数限制、退避策略、幂等保障和失败记录，禁止无限重试。
4. 幂等规则。创建、支付、扣减、通知、回调、消息消费等操作应具备幂等控制。
5. 并发规则。共享状态、缓存、文件、数据库记录和后台任务应有同步、锁、事务或串行化策略。
6. 生命周期规则。后台线程、异步任务、定时任务和外部连接应有启动、关闭、等待完成和异常收集机制。
7. 失败恢复规则。失败后应释放资源、回滚状态、记录错误并支持重试或补偿。
8. 降级规则。降级不得掩盖关键业务失败，不得返回虚假成功。
9. 资源耗尽规则。队列、连接池、线程池、缓存、文件句柄和内存使用应有上限和保护。
10. 环境适配规则。配置缺失、权限不足、依赖不可达和启动失败应有明确错误信息和安全退出策略。

#### 证据要求

应收集异常处理代码、超时配置、重试代码、并发访问点、资源分配释放点、线程和任务管理代码、连接池配置、关闭流程、日志和恢复路径。

#### AI 判定口径

AI 应重点识别无超时等待、无限重试、异步任务无人管理、全局可变状态、异常吞没、资源未释放和失败后继续执行等问题。可靠性问题的严重性应结合业务关键性和生产触发概率判定。

#### 整改要求

可靠性高风险问题应优先在发布前修复。整改后应补充异常、超时、并发、关闭和恢复场景测试，并在报告中记录复测结果。

### 5.6 可维护性与可演进性

#### 管理目标

确保代码结构易理解、易测试、易替换、易扩展，避免技术债阻碍业务演进、安全加固和长期运营。

#### 审计规则

1. 单一职责规则。类、函数、模块应有明确职责，避免同时承担业务编排、数据访问、配置解析、外部调用和展示逻辑。
2. 可测试性规则。核心逻辑应能通过单元测试或集成测试验证，避免强依赖静态状态、真实外部环境或不可替换组件。
3. 依赖注入规则。外部依赖、策略组件、客户端和配置应可替换，避免硬编码具体实现。
4. 抽象质量规则。抽象应服务于真实变化点，避免过度抽象或接口与实现一一对应的空洞抽象。
5. 配置管理规则。配置项应集中、命名清晰、有默认值和校验，避免散落在代码和脚本中。
6. 文档规则。关键业务规则、架构约束、部署步骤、配置含义和风险接受应有文档记录。
7. 技术债规则。临时方案、兼容补丁、废弃接口和绕过逻辑应标注原因、责任人和清理计划。
8. 全局状态规则。全局可变状态应收敛到上下文对象、依赖注入容器或明确同步策略。
9. API 演进规则。公共接口变更应考虑兼容性、版本化、迁移说明和调用方影响。
10. 可读性规则。代码组织应支持审阅者快速理解主要流程、错误路径和依赖关系。

#### 证据要求

应收集模块结构、核心类和函数、依赖关系、接口定义、配置文件、测试代码、文档、技术债注释、变更历史和调用关系。

#### AI 判定口径

AI 应避免以“看起来复杂”为理由直接判定问题，应说明复杂性如何影响测试、变更、安全或稳定性。维护性问题只有在影响范围明确时才应提升严重等级。

#### 整改要求

维护性整改应与业务需求或风险修复结合推进，避免无目标的大规模重构。对高耦合核心模块，应先补充测试和接口契约，再分阶段重构。

### 5.7 可观测性与运维性

#### 管理目标

确保系统在开发、测试、生产和故障处理阶段具备足够的日志、指标、链路、错误码、健康检查和审计能力，支持快速定位和恢复。

#### 审计规则

1. 日志上下文规则。关键异常日志应包含操作名、对象标识、请求或任务上下文、状态、错误码、异常类型和恢复动作。
2. 日志安全规则。日志不得输出密码、令牌、密钥、身份证件、银行卡、完整连接串等敏感信息。
3. 指标规则。关键业务流程、外部依赖、队列、重试、降级、错误率、耗时和资源使用应有指标。
4. 链路追踪规则。跨服务调用应传递 trace 信息，支持定位请求路径和瓶颈。
5. 错误码规则。面向用户、运维和开发的错误应有稳定错误码或分类，避免只有泛化错误文本。
6. 告警规则。高影响失败、依赖不可用、积压、频繁重试、降级和资源耗尽应有告警或监控信号。
7. 健康检查规则。服务应提供启动、存活、就绪和关键依赖健康检查。
8. 审计日志规则。权限变更、登录失败、敏感数据访问、配置变更和高风险操作应有审计记录。
9. 运行状态规则。运行时关键状态应可查询，但不得暴露敏感信息或内部攻击面。
10. 故障演练规则。关键系统应有故障演练、应急预案或恢复验证记录。

#### 证据要求

应收集日志代码、日志样例、指标配置、监控规则、追踪配置、错误码定义、健康检查接口、审计日志、告警策略和运维文档。

#### AI 判定口径

AI 应关注“故障发生后能否定位、能否告警、能否恢复”。对于非生产或小型工具，可适当降低要求；对在线服务、批处理、网关、设备控制系统，应提高可观测性要求。

#### 整改要求

缺少关键错误上下文、健康检查或审计日志的问题，应结合生产风险安排整改。整改应避免过度日志化和敏感信息泄露。

### 5.8 性能与资源

#### 管理目标

确保系统在预期负载下具备合理的响应时间、吞吐能力和资源使用效率，避免算法、数据库访问、阻塞调用和资源生命周期问题导致性能退化。

#### 审计规则

1. 数据库访问规则。避免 N+1 查询、无索引高频查询、全表扫描、不必要的事务持有和大结果集一次性加载。
2. 算法复杂度规则。核心路径应避免不必要的高复杂度算法，循环嵌套和集合操作应符合数据规模。
3. 内存规则。大对象、缓存、集合、流处理和文件读取应控制内存占用，避免无界增长。
4. I/O 规则。文件、网络和数据库 I/O 应减少重复操作，必要时批量化、缓存或异步处理。
5. 缓存规则。缓存应有命中率目标、容量限制、过期策略和一致性策略。
6. 阻塞调用规则。异步或高并发路径中应避免不必要的阻塞等待和同步锁竞争。
7. 连接池规则。数据库、HTTP、消息队列和线程池配置应与负载匹配，并有资源上限。
8. 大文件规则。大文件上传、下载、解析和导出应采用流式处理或分块处理。
9. 热路径规则。高频调用路径应避免重复解析配置、重复创建重资源对象和不必要日志。
10. 性能测试规则。关键性能假设应有压测、基准测试或生产指标支撑。

#### 证据要求

应收集数据库查询、ORM 映射、循环逻辑、缓存代码、连接池配置、线程池配置、文件处理代码、性能测试结果、监控指标和生产容量数据。

#### AI 判定口径

AI 应结合数据规模和调用频率判断性能问题，避免把低频管理接口中的小问题夸大。没有负载数据时，可列为“待复核”或“优化建议”。

#### 整改要求

性能整改应先确认瓶颈和数据规模，再选择索引、批量化、缓存、异步、流式处理或算法优化。整改后应通过指标或测试结果验证。

### 5.9 测试质量

#### 管理目标

确保测试能够覆盖核心业务、边界条件、异常路径、并发行为、回归缺陷和发布门禁，支持安全整改和持续演进。

#### 审计规则

1. 单元测试规则。核心纯逻辑、状态转换、边界判断和算法应有单元测试。
2. 集成测试规则。数据库、外部服务、消息队列、缓存和配置加载应有集成测试或替代验证。
3. 边界测试规则。空值、极值、重复输入、非法输入、过期数据和分页边界应覆盖。
4. 异常测试规则。依赖失败、权限不足、网络超时、数据不一致和资源不足应有测试。
5. 并发测试规则。幂等、重复提交、竞态条件、锁和事务冲突应有并发测试或专项验证。
6. 回归测试规则。历史缺陷和高风险整改应有回归测试。
7. 安全测试规则。认证授权、注入、路径穿越、敏感信息和反序列化等高风险点应有安全测试或扫描。
8. CI 接入规则。测试应纳入 CI，失败应阻断或至少形成风险记录。
9. 测试可维护规则。测试应稳定、可重复、隔离外部环境，避免依赖执行顺序和真实生产资源。
10. 覆盖解释规则。覆盖率低不必然代表高风险，但核心路径无测试应视为交付风险。

#### 证据要求

应收集测试目录、测试文件、测试报告、覆盖率报告、CI 日志、历史缺陷、回归测试、Mock 或测试容器配置和失败记录。

#### AI 判定口径

AI 应重点判断“关键风险是否被测试覆盖”，而不是只看覆盖率数字。对没有测试的核心业务路径，应说明风险路径、缺失场景和建议测试类型。

#### 整改要求

高风险修复必须补充对应测试或说明不可测试原因。发布前应至少保证核心流程、异常路径和安全修复具备可复测材料。

### 5.10 依赖与供应链风险

#### 管理目标

识别第三方依赖、构建插件、工具链、容器镜像和制品来源中的漏洞、许可证、完整性和可追溯风险，保证交付物可复现、可追踪、可治理。

#### 审计规则

1. 漏洞规则。依赖漏洞应记录依赖名称、版本、漏洞编号、严重等级、可达性或使用场景。
2. 版本规则。依赖版本应固定或受锁文件约束，避免不受控漂移。
3. 锁文件规则。npm、pip、Poetry、Maven、Gradle、Go、Cargo、Composer 等生态应保留合适的锁定机制或版本约束。
4. SBOM 规则。发布制品应生成 SBOM，优先采用 CycloneDX 或 SPDX 格式。
5. 许可证规则。依赖许可证应符合项目交付要求，限制性许可证应经过审批。
6. 构建插件规则。构建插件、下载脚本和代码生成工具应来自可信源，并锁定版本。
7. 容器镜像规则。基础镜像应明确来源、版本和漏洞状态，避免使用过期或未知镜像。
8. 工具链规则。编译器、包管理器、扫描工具和脚本应记录版本和来源。
9. 私有源规则。私有包源、镜像源和代理应有访问控制和完整性保障。
10. 例外规则。无法升级的漏洞依赖应记录补偿控制、不可达性证据、责任人和例外有效期。

#### 证据要求

应收集依赖清单、锁文件、SBOM、漏洞扫描结果、许可证扫描结果、容器镜像信息、构建脚本、下载脚本、包源配置和例外审批记录。

#### AI 判定口径

AI 应区分“存在漏洞依赖”和“漏洞可达或影响当前制品”。无法确认可达性时，应标记为“待复核”或要求提供调用证据。高危漏洞、可达漏洞和无补偿控制的过期组件应优先处理。

#### 整改要求

供应链高风险问题应优先升级到修复版本；无法升级时，应记录补偿控制、不可达性证据、责任人和复审日期。发布制品应保留 SBOM 和扫描结果。

## 6. 基线、误报与风险接受

### 6.1 状态定义

| 状态 | 含义 | 是否计入有效风险 | 是否触发高危门禁 |
|---|---|---:|---:|
| open | 已确认或待处理的问题。 | 是 | 是 |
| accepted | 业务确认暂不修复，已有责任人、缓解措施和期限。 | 是 | 否 |
| false_positive | 经复核为误报。 | 否 | 否 |
| fixed | 已修复并通过复测。 | 否 | 否 |

`accepted` 不等于误报，仍应保留在报告中并计入评分，仅用于 CI 高危门禁豁免。

### 6.2 误报处理

标记为误报必须说明：

1. 原始发现来源。
2. 误报证据。
3. 复核人。
4. 复核时间。
5. 是否需要保留后续监控。

### 6.3 风险接受

风险接受必须说明：

1. 接受原因。
2. 业务影响。
3. 补偿控制。
4. 责任人。
5. 有效期限。
6. 复审日期。
7. 是否影响发布或对外交付。

### 6.4 基线管理

1. 首次全量审计后可导出初始基线。
2. 基线中不应手工修改问题指纹。
3. 只允许更新状态、原因、责任人和复审信息。
4. 已修复问题在下一次审计不再出现时，可从基线中清理。
5. 基线变化应随审计报告一并归档。

## 7. CI 门禁与发布控制

### 7.1 默认门禁

建议 CI 门禁采用以下策略：

1. 存在 open 状态高风险问题时，阻断发布。
2. 总分低于发布阈值时，阻断发布。
3. 缺少构建、测试或关键扫描结果时，记录限制项，并由技术负责人确认是否允许继续。
4. accepted 状态高风险问题不触发高危门禁，但必须在报告中展示。

### 7.2 发布前要求

发布前应满足：

1. P0 问题已修复或完成正式风险接受。
2. P1 问题有明确整改计划和责任人。
3. 安全、正确性、可靠性和供应链高风险已复核。
4. 核心修复已有测试或复测记录。
5. 报告限制项已被审计发起人和技术负责人知悉。

## 8. AI 审计提示词与执行规范

### 8.1 输入要求

向 AI 发起审计时，应尽量提供：

1. 仓库路径。
2. 审计目标。
3. 变更范围。
4. 业务背景。
5. 重点关注维度。
6. 排除目录。
7. 是否生成 HTML 报告。
8. 是否启用 SBOM、SCA、脱敏和门禁。

### 8.2 AI 输出要求

AI 输出应遵守：

1. 先列高风险和中风险问题，再列低风险优化。
2. 每项问题必须有证据。
3. 工具发现必须复核后再进入最终报告。
4. 待复核项必须说明缺少什么证据。
5. 不确定时不得使用确定性措辞。
6. 不得把未覆盖范围当成已审计结论。
7. 不得擅自修改被审计项目。

### 8.3 问题描述模板

每项问题建议按以下结构描述：

```text
标题：
严重等级：
审计维度：
优先级：
置信度：
位置：
证据：
影响：
建议：
规则 ID：
来源：
指纹：
状态：
影响组件：
修复工作量：
CWE/OWASP：
```

## 9. 审计档案管理

审计档案应至少保存：

1. 审计配置。
2. 项目盘点结果。
3. 工具命令和输出。
4. 结构化审计 JSON。
5. HTML 报告。
6. SBOM 和 SCA 结果。
7. 基线文件。
8. 风险接受记录。
9. 复测记录。
10. 限制项说明。

对外交付报告应按需脱敏，移除本机路径、内部网络地址、用户名、敏感配置和不可公开证据，但应保留相对位置、规则 ID、证据摘要和整改建议。

## 10. 附：10 维度快速检查表

| 维度 | 核心检查点 | 主要证据 |
|---|---|---|
| 架构合理性 | 分层、依赖方向、模块边界、事务边界、缓存、分布式失败、配置设计。 | 目录结构、依赖关系、接口、事务代码、配置、设计文档。 |
| 功能正确性 | 业务规则、状态转换、边界条件、默认值、fallback、数据一致性、异常分支。 | 业务文档、核心逻辑、状态代码、测试、缺陷记录。 |
| 编码规范性 | 命名、注释、复杂度、异常、资源释放、魔法值、重复代码、语言惯用法。 | 代码片段、静态分析、格式配置、复杂度指标。 |
| 安全性 | 认证授权、注入、XSS、SSRF、路径穿越、反序列化、密钥、加密、审计日志。 | 入口代码、权限代码、查询和命令执行点、扫描结果、配置。 |
| 可靠性与健壮性 | 空值、超时、重试、幂等、并发、生命周期、失败恢复、资源上限。 | 异常处理、超时配置、任务管理、连接池、关闭流程。 |
| 可维护性与可演进性 | 单一职责、可测试、依赖注入、抽象质量、配置管理、文档、技术债。 | 模块结构、接口、测试、配置、文档、变更历史。 |
| 可观测性与运维性 | 日志上下文、指标、追踪、错误码、告警、健康检查、审计日志。 | 日志代码、监控配置、错误码、健康检查、运维文档。 |
| 性能与资源 | 数据库访问、算法复杂度、内存、I/O、缓存、阻塞调用、连接池、大文件。 | 查询代码、循环逻辑、缓存代码、连接池配置、性能数据。 |
| 测试质量 | 单元、集成、边界、异常、并发、回归、安全测试、CI 接入。 | 测试目录、测试报告、覆盖率、CI 日志、复测记录。 |
| 依赖与供应链风险 | 漏洞、版本锁定、SBOM、许可证、构建插件、容器镜像、工具链、例外。 | 依赖清单、锁文件、SBOM、扫描结果、构建脚本、镜像信息。 |
